@model IList<PaulaPires.Models.ModeloFAQ>
@if (Model.Count > 0)
{
    <div class="portlet-body">
        <h4 class="block">
            Lista de perguntas e respostas da página</h4>
        <table class="table">
            <thead>
                <tr>
                    <th>
                        Pergunta
                    </th>
                    <th>
                        Resposta
                    </th>
                    <th>
                        Ordem
                    </th>
                    <th>
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Pergunta
                        </td>
                        <td>@item.Resposta
                        </td>
                        <td>@item.Orderby
                        </td>
                        <td>
                            <a href="@Url.Action("Cadastrar", "ConteudoFAQ", new { area = "administrador", pCadastro = @item.Id })" class="btn default btn-xs green filter-cancel remover" >
                                <i class="fa fa-edit"></i>Editar</a> <a class="btn default btn-xs red filter-cancel remover-faq" data-value="@item.Id" id="remover">
                                    <i class="fa fa-times"></i>Remover</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
}
