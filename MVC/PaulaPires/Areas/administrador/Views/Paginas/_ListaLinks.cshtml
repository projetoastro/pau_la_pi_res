@model IList<PaulaPires.Models.ModeloLinks>
@if (Model.Count > 0)
{
    <div class="portlet-body">
        <h4 class="block">
            Lista de links da página</h4>
        <table class="table">
            <thead>
                <tr>
                    <th>
                        Pergunta
                    </th>
                    <th>
                        Resposta
                    </th>
                    <th>
                        Ordem
                    </th>
                    <th>
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Conteudo
                        </td>
                        <td>@item.Url
                        </td>
                        <td>@item.Orderby
                        </td>
                        <td>
                            <a href="@Url.Action("Cadastrar", "ConteudoLinks", new { area = "administrador", pCadastro = @item.Id })" class="btn default btn-xs green filter-cancel remover" >
                                <i class="fa fa-edit"></i>Editar</a> <a class="btn default btn-xs red filter-cancel remover-links" data-value="@item.Id" id="remover">
                                    <i class="fa fa-times"></i>Remover</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
}

